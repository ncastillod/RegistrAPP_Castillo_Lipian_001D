import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Experiencia-1A',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
